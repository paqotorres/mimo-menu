import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Publicado en GitHub Pages con dominio propio: https://mimoestudio.mx
  site: 'https://mimoestudio.mx',
});
